import 'package:flutter/material.dart';

abstract class AppColors {
  // Primary palette (from design)
  static const primary = Color(0xFF6C63FF);
  static const primaryDark = Color(0xFF5850EC);
  static const primaryMid = Color(0xFF9B8CFF);
  static const primaryLight = Color(0xFFD6CCFF);
  static const primarySurface = Color(0xFFF9FAFE);

  // Neutrals
  static const dark = Color(0xFF1A1A1A);
  static const darkGrey = Color(0xFF4F4F4F);
  static const grey = Color(0xFF9E9E9E);
  static const lightGrey = Color(0xFFE0E0E0);
  static const background = Color(0xFFF9FAFE);
  static const white = Color(0xFFFFFFFF);

  // Semantic
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFE53935);
  static const warning = Color(0xFFFF9800);

  // Card
  static const cardBg = Color(0xFFFFFFFF);
}